meta {
  name: Regras - Inserir
  type: http
  seq: 2
}

post {
  url: {{URL_SERVER}}/regras
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {    
    "categoriaId":7,
    "descricao":"STEPHANY MUNDO DAS DELICIAS",  
    "termos":["STEPHANY","MUNDO DAS DELICIAS", "STP MUND DEL"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
