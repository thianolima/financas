meta {
  name: Regras - Listar
  type: http
  seq: 3
}

get {
  url: {{URL_SERVER}}/regras
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
