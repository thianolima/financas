meta {
  name: Despesas - Processar Regras
  type: http
  seq: 4
}

post {
  url: {{URL_SERVER}}/despesas/processar-regras
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "despesasIds" : [6350, 6348]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
