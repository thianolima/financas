meta {
  name: Tag - Alterar
  type: http
  seq: 2
}

put {
  url: {{URL_SERVER}}/tags/39
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {    
    "nome":"THIANO"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
