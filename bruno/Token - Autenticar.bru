meta {
  name: Token - Autenticar
  type: http
  seq: 4
}

post {
  url: {{URL_SERVER}}/tokens
  body: json
  auth: inherit
}

body:json {
  {
    "email": "thianolima@gmail.com",
    "senha": "123456"
  }
}

script:post-response {
  bru.setVar("TOKEN", res.body.accessToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
